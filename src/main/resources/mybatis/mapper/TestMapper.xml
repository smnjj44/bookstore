<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.bookstore.dao.TestMapper">
    <insert id="insert" parameterType="com.springboot.bookstore.dao.TestMapper">
        insert into wndz(wid,name,url,date,path) values(#{wid},#{name},#{url},#{date},#{path})
    </insert>
</mapper>